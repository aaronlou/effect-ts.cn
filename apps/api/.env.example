# apps/api 环境变量示例。复制为 .env 后生效（本骨架由 bootstrap/Config 读取）。

# HTTP 监听端口
API_PORT=8787

# 可选：数据库连接串。
# 留空 => 使用进程内 InMemory 仓储（便于无 DB 跑通骨架）；
# 设置   => 启动时自动建表并切换为 Postgres 仓储（见 infra/docker-compose.yml）。
# 本地一行起库：pnpm db:up
DATABASE_URL=
