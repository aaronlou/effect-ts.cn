---
import Base from "../layouts/Base.astro"
import Mascot from "../components/Mascot.astro"
import ApiHealth from "../components/ApiHealth"
---

<Base
  title="Effect 中文社区 — 官方文档中文翻译 · 可溯源 AI 问答"
  description="Effect（TypeScript 的 effect system）官方文档全量中文翻译：v4 + v3 共 234 页，每页标注上游基线。站内 AI 问答的每条引用都能点回原文，没有依据就拒答。另有 26 个用 Effect 写的开源 AI / Agent 项目推荐。"
>
  <section class="hero">
    <div class="container">
      <h1>面向 AI 时代的可靠 TypeScript，<br />用中文把它讲清楚。</h1>
      <p class="lead">
        先把官方文档读明白：每一页译文都可追溯到上游 commit，落后即标注；未翻译的页面直接给出
        英文原文与认领入口。更进一步 —— 站内回答<strong>逐句带引用</strong>，没有依据就直说不知道，
        还能直接接进你的编码 Agent。
      </p>
      <div class="cta-row">
        <a class="btn btn-primary" href="/docs/">开始读文档</a>
        <a class="btn btn-ghost" href="/ask/">问文档 <kbd>⌘I</kbd></a>
        <a class="btn btn-ghost" href="/debug/">报错诊断</a>
        <a class="btn btn-ghost" href="https://github.com/Effect-TS/effect" rel="external">GitHub ↗</a>
      </div>
      <div style="margin-top: 1rem; font-size: 0.9rem">
        <ApiHealth client:load />
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <h2>内容结构（站点地图）</h2>
      <div class="card-grid">
        <div class="card">
          <h3><a href="/docs/">文档译站</a></h3>
          <p>跟随官方 v4 主线 + v3 切换；精译 + 机器辅助分层；翻译进度公开可查。</p>
        </div>
        <div class="card">
          <h3><a href="/blog/">博客</a></h3>
          <p>官方 Blog 译站 + 社区原创；投稿走 GitHub PR，规范见译者指南。</p>
        </div>
        <div class="card">
          <h3><a href="/community/">社区</a></h3>
          <p>AI 问答已上线（可溯源）；人之间的问答、活动日历与贡献者榜正在建设。</p>
        </div>
        <div class="card">
          <h3>教程与小抄</h3>
          <p>从 Promise 到 Effect 的学习路径、中英术语对照、可运行的 Playground 示例。</p>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <h2>AI 原生：答案可溯源，且能被 Agent 调用</h2>
      <div class="mascot-intro">
        <Mascot size={72} />
        <p>
          我是<strong>小效</strong>，本站的常驻助手（右下角那只猫就是我）。
          <strong>猫有九条命，就像 Fiber 很轻</strong>——所以我一直在这儿，随时可以问我。
        </p>
      </div>
      <p style="color: var(--fg-muted); max-width: 72ch">
        不是"给文档挂个聊天框"：答案只从站内已审校的中文译文里检索，逐条给出
        <strong>页面 + 小节锚点 + 上游基线</strong>；没有依据会直接拒答，并区分"文档里没有"与"中文还没翻译"。
      </p>
      <div class="card-grid" style="margin-top: 1.25rem">
        <div class="card">
          <h3><a href="/ask/">问文档 ⌘I →</a></h3>
          <p>在任意译文页就地提问；答案带可点击的引用，点开直达对应小节。</p>
        </div>
        <div class="card">
          <h3><a href="/debug/">报错诊断 →</a></h3>
          <p>粘贴 Effect 报错，提取其中的 API 与错误码，定位到相关中文小节。</p>
        </div>
        <div class="card">
          <h3>
            <a
              href="https://github.com/aaronlou/effect-ts.cn/blob/main/docs/agent-integration.md"
              rel="external">接进你的 Agent →</a
            >
          </h3>
          <p>
            MCP Server（<code>pnpm mcp</code>）、<code>/api/knowledge/ask</code>、
            <code>/llms-full.txt</code>、<code>/docs/&lt;slug&gt;.md</code> 四种形态。
          </p>
        </div>
        <div class="card">
          <h3><a href="/glossary/">术语门禁 →</a></h3>
          <p>AI 输出与译文受同一套术语约束：违反社区术语就回退，不让模型自创译名。</p>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <h2>为什么值得关注 Effect？</h2>
      <div class="card-grid">
        <div class="card">
          <h3>类型化的错误</h3>
          <p>函数签名里就能看到它会怎么失败；重试、回退、超时都变成普通 API。</p>
        </div>
        <div class="card">
          <h3>显式的依赖</h3>
          <p>需要什么服务写进类型，测试时用 Layer 整块替换 —— 本站后端就是活例子。</p>
        </div>
        <div class="card">
          <h3>结构化并发</h3>
          <p>Fiber 让并发可组合、可取消、可清理，不再有失控的 Promise.all。</p>
        </div>
        <div class="card">
          <h3>Schema 单一来源</h3>
          <p>校验、序列化、API 契约、OpenAPI 从一份 Schema 生成，前后端零重复。</p>
        </div>
      </div>
    </div>
  </section>
</Base>
