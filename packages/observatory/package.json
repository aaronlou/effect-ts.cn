{
  "name": "@ecn/observatory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Effect × AI Agent 生态观测台：可复现的 GitHub 生态数据研究（候选发现 → 分类 → 快照 → 报告）",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "discover": "tsx src/cli.ts discover",
    "report": "tsx src/cli.ts report",
    "scan": "tsx src/cli.ts scan",
    "classify": "tsx src/cli.ts classify"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.23.13",
    "typescript": "^5.6.0",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "@ecn/content": "workspace:*"
  }
}
