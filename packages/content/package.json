{
  "name": "@ecn/content",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "内容管线 CLI：译文同步状态扫描 / upstream hash 比对（后续）+ 术语抽检 —— “同步即信誉”的工程落点",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(content: Phase 1 补 sync/stale 单测)\"",
    "status": "tsx src/cli.ts status"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.23.13",
    "typescript": "^5.6.0"
  }
}
