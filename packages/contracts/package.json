{
  "name": "@ecn/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "前后端共享契约：Effect Schema DTO / 错误码（Schema-first，单一事实来源）",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(contracts: schema 自身即测试对象，Phase 1 补 encode/decode 用例)\""
  },
  "dependencies": {
    "@effect/schema": "^0.75.5",
    "effect": "^3.22.2"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
